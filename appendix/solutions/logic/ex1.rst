Exercise 1.1
============

The statement :math:`P \land Q` is false, since it would require both :math:`P`
and :math:`Q` to be true: in this case, :math:`Q` is "pigs can fly", which is
(thankfully) false.
